# Broker configuration for the rig, installed to /etc/mosquitto/conf.d/stem.conf.
#
# The broker belongs to Stem. Stem is the health daemon that stays alive and reporting while
# the brain is down — which is precisely when health matters — and the transport its reports
# travel on is part of that job rather than a service the brain happens to offer. Stem's own
# binary does not exist yet; this file is the first piece of it to land, and it moves under
# whatever Stem grows into rather than staying somewhere neutral.
#
# Debian's own mosquitto.conf is left untouched; this is an overlay in the include directory
# it already reads, so a package upgrade cannot quietly revert the rig's settings and this
# file cannot quietly inherit changes to the distribution's defaults.
#
# The version is whatever Debian ships. A broker moving a few hundred bytes a second around
# one Pi has no use for anything newer, and a package that upgrades with the OS is one fewer
# thing to maintain on a machine that must come up unattended.

# One listener on every interface, rather than a stanza per interface.
#
# Not laziness — a listener bound to a named address fails at startup if that address does not
# exist yet, and wg0 is brought up by wg-quick during boot. Binding the tunnel address would
# make the broker's startup a race against the tunnel's, lost silently and only on the reboots
# where the modem is slow. The wildcard has no such ordering to lose.
#
# What that exposes is a read-only telemetry tree, reachable over the WireGuard tunnel and the
# rig's own AP. Nothing subscribes from off the rig and no topic under it commands anything;
# eth0 is the camera's dedicated link and has no other host on it.
listener 1883 0.0.0.0

# No accounts. Both publishers are local processes and every subscriber is either on the
# point-to-point tunnel or associated with the AP, so a password file here would be a
# credential to distribute and rotate in exchange for no boundary that is not already there.
allow_anonymous true

# Persistence is left at the distribution's default, deliberately. Retained topics are the
# slow ones — identity, inventory, last known state — and they are what makes a client that
# joins after the interesting moment see a live picture rather than an empty tree. They have
# to survive a broker restart to do that job at all.
